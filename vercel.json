{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/:path((?!api/).*)", "destination": "/index.html" }
  ],
  "crons": [
    { "path": "/api/cron/news-notifications", "schedule": "0 14 * * *" }
  ]
}
